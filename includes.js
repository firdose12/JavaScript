/*includes will tell you whether the particular element exists in an array or not
generally it uses for small arrays */

const items = [1, 2, 3, 4, 5, 6];

const output = items.includes(3);
console.log(output);